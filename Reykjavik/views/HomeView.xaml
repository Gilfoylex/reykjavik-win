<UserControl x:Class="Reykjavik.views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ct="clr-namespace:UIBase.controls;assembly=UIBase"
             xmlns:local="clr-namespace:Reykjavik.views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <ct:TextButton Width="50" Height="25" Style="{DynamicResource BtnStyle}" Click="ButtonBase_OnClick">连接</ct:TextButton>
            <ct:TextButton Margin="20 0" Width="50" Height="25" Style="{DynamicResource BtnStyle}">断开</ct:TextButton>
        </StackPanel>
    </Grid>
    
</UserControl>
