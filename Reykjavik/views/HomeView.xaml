<UserControl x:Class="Reykjavik.views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ct="clr-namespace:UIBase.controls;assembly=UIBase"
             xmlns:local="clr-namespace:Reykjavik.views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <ct:TextButton Command="{Binding TestStartCommand}">启动Xray</ct:TextButton>
            <ct:TextButton Command="{Binding TestStopCommand}">停止Xray</ct:TextButton>
            <ct:TextButton Click="ButtonBase_OnClick" >添加连接</ct:TextButton>

            
        </StackPanel>
    </Grid>
    
</UserControl>
